import { ISSFCompetitionSyncClient } from "./issf-competition-sync-client";

export const metadata = { title: "Admin · ISSF Takmičenja" };

export default function ISSFCompetitionSyncPage() {
  return <ISSFCompetitionSyncClient />;
}
