import { CompetitionFormClient } from "./competition-form-client";

export const metadata = { title: "Admin · Novo takmičenje" };

export default function NovoTakmicenjePage() {
  return <CompetitionFormClient />;
}
